import { Stack, Typography } from "@mui/material";

export const TopPanel = () => (
  <Stack sx={{ height: "50vh !important", backgroundColor: theme => theme.palette.common.black }}>
    <Typography>GAME</Typography>
  </Stack>
);
